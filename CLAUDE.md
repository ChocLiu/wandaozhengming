# 万道争鸣（wandaozhengming）

机制驱动的开放沙盒修仙 RPG。Godot 4.7（GDScript）。策划文档全部用中文。

## 核心设计原则（写任何代码/文档前先记住）

1. **道即机制**：功法/种族/境界带来的是玩法规则的变化，不是数值变化。禁止「攻击 +10%」式的功法。
2. **万法皆可组**：不同机制的功法通过「六大交互面」（状态/资源/场地/单位/时序/部位）自由组合，组合空间是核心乐趣。
3. **反套路**：无抽卡、无经验条、无数值膨胀、无词条装备、死亡是内容（死亡三途）。
4. **先深后宽**：3 个机制做深 > 30 个机制做浅。

## 目录约定

- `docs/` 策划文档（策划先行）；`godot/` Godot 工程；`art/` `audio/` 源文件；`reference/` 不入库。
- 当前阶段：P0 策划与原型准备期。路线图见 `docs/05_发行与路线图/路线图与MVP范围.md`。

# 工具选择速查规则

- **微调 + 调试 + 验证** → MCP
- **批量 + 结构 + 基建** → 直接改文件

## 用 MCP 工具（mcp__godot-mcp__*）当：
- 操作当前活动场景里的节点（位置、属性）
- 读取/修改游戏运行时的变量值
- 需要自动运行游戏、截图、模拟输入来验证
- 重命名/移动资源，且要自动修复所有引用

## 直接编辑文件（write_to_file / replace_in_file）当：
- 全局批量替换或重构（如改所有 get_node 为 %）
- 定义新的类、枚举、常量
- 新建脚本或场景的基础框架
- 处理 Git 冲突标记
